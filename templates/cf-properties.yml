meta:
  domain: (( merge || properties.domain ))

properties:
  domain: (( meta.domain ))
  system_domain_organization: system

  dea_next:
    disk_mb: 81960
    memory_mb: 100000

  uaa:
    scim:
      userids_enabled: true
